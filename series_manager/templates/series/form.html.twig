{% extends 'base.html.twig' %}

{% block title %}{{ serie is defined ? 'Editar' : 'Nova' }} Série{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.serieName) }}
    {{ form_row(form.seasonsQuantity) }}
    {{ form_row(form.episodesPerSeason) }}
    {{ form_widget(form.save, {'attr': {'class': 'btn-dark'}}) }}
    {{ form_end(form) }}
{% endblock %}

